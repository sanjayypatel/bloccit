<p>
  <span class="glyphicon glyphicon-comment" aria-hidden="true"></span>
  <%= comment.body %>
  <small>
    <%= time_ago_in_words(comment.created_at) %> ago by <%= post.user.name %>
  </small>
</p>